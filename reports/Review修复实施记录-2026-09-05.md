# Review 修复实施记录（2026-09-05）

本次按整体 Review 的 P1 → P2 顺序修复，三个子 agent 分别处理 Web、存储和画像输入，主任务处理每日编排、持久队列、工作流和集成验证。所有变更保留在工作区，未提交或推送 Git，未发送真实通知或调用模型服务。

## 已完成

| Review 项目 | 修复结果 |
|---|---|
| P1 历史日期写成当前榜 | 严格验证日期；未来日拒绝；历史日只回放 canonical/legacy；缺数据报错；refresh 仅允许今天且不可与 notify-only 混用 |
| P1 通知失败任务假成功 | 通道拒绝/网络错误记录 failed 并抛错；日报、周报分别尝试后汇总失败，CLI 非零退出；成功事件不被后续兼容日志故障覆盖 |
| P1 刷新残留旧榜尾 | 单事务清除目标日全部受管真实榜，再插入新批次；删除旧名次及被移除的语言榜，随后刷新聚合和索引 |
| P2 归档丢失去重 | 投递事件读取归档与 live；旧 push 去重读取归档并过滤混存事件；历史发送→轮转→重跑测试验证不重复发送 |
| P2 周报混入未来数据 | 趋势及新面孔限定起止日期；画像生成时间使用北京时间半开区间，支持 Z/时区偏移时间戳 |
| P2 堆叠图覆盖 | 每季度独立累计，单季度和多季度 SVG 几何断言验证位置 |
| P2 摘要丢失已有画像 | 通知和预览统一加载已有 one_liner；摘要与周报附快照标识 |
| P2 今日快照自愈不可达 | 捕获/校验/修复先于重建；Actions 数据审计移到捕获之后；内容损坏但旧 id 恰好与新 id 相同也会归档替换 |
| P2 重建丢失榜单元数据 | canonical/legacy 恢复 description/language；首次观测保留、同日总榜优先，可靠 API/CSV 不受覆盖 |
| P2 积压无法跨日补齐 | pending_queue.json 持久化 attempts/status/retry_at；当天优先、余量补历史真实榜积压；临时错误次日重试、无 README 30 天复查 |

另完成：FTS 删除/创建/填充置于同一 SAVEPOINT，失败保留旧索引；画像 hash 与实际模型输入统一，处理异常成功响应；README 缓存原子写入，403/5xx/空 200 不再误标永久无 README；readyz 缺库返回 503；首页展示采集新鲜度和画像覆盖率，详情显示身份风险；缺口提示动态计算；README 更新历史覆盖范围和运行语义；测试默认禁止 requests 真实出网。

## 验证结果

- `python -m pytest -p no:cacheprovider`：**203 passed**，较基线增加 46 项；仍有 1 条 Starlette/httpx 测试依赖弃用警告。
- `python -m ruff check .`：通过。
- `git diff --check`：通过（Git 提示 Windows 换行转换，无 whitespace 错误）。
- 真实 source 重建成功：24,225 仓库、82,237 趋势记录、1,642 个数据日期、1,179 篇画像。
- `python -X utf8 scripts/audit_data.py`：无硬错误，integrity=ok。
- 重建后本地 TestClient 检查首页、readyz、agent 搜索、浏览页、趋势页、Jarred-Sumner/bun 详情页：全部 HTTP 200。
- 特别回归包括：历史/未来日禁止实时抓取、CLI 今日坏快照恢复、刷新 25→20 及删除语言榜、通知拒绝导致 CLI 失败、成功后日志故障不重发、归档后不重发、历史周报及 UTC 边界、分阶段摘要画像、队列跨重建补齐与 README 冷却期。

## 保留的边界

- 历史同名复用风险与重复元数据没有可靠证据可自动合并：审计仍报告 34 个日期矛盾仓库、85 个重复元数据仓库（61 个字段冲突）、7 条疑似刷星记录和 2026-08-30 的旧推送差异；未改写这些 source。
- 本地真实快照最新日期仍为 2026-09-02，页面会提示新鲜度；此次未触发外部采集或远端 Actions。新增画像队列将在下一次非 dry-run capture 中初始化并处理。
- 飞书重试仍无法消除“服务端成功但客户端未收到确认”的重复窗口；快照标识与投递事件用于识别和恢复。
- 周报时间范围已经修正，但画像仍取当前数据库版本；没有实现历史报告正文/画像版本永久冻结。
- 本次没有进行性能压测或获取真实 GitHub 页面 fixture；采集解析仍以离线样本回归，后续可积累脱敏真实页面。
